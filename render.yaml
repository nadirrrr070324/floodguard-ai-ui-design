services:
  - type: web
    name: floodguard-portal-api
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
    autoDeploy: true
